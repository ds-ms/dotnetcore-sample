on: [push]
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    # Connect to Azure Container registry (ACR)
    - uses: azure/docker-login@v1
      with:
        login-server: docker.pkg.github.com
        username: ds-ms 
        password: ${{ secrets.GITHUB_TOKEN }}
    
    # Container build and push to a Azure Container registry (ACR)
    - run: |
        docker pull docker.pkg.github.com/ds-ms/go-sample/app:1.0
        docker tag docker.pkg.github.com/ds-ms/go-sample/app:1.0 docker.pkg.github.com/ds-ms/dotnetcore-sample/app2:2.0
        docker push docker.pkg.github.com/ds-ms/dotnetcore-sample/app2:2.0
